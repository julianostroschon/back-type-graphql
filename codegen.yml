overwrite: true
schema: 'http://localhost:4001/graphql'
generates:
  src/typedefs/graphql.ts:
    plugins:
      - 'typescript'
      - 'typescript-resolvers'
  ./graphql.schema.json:
    plugins:
      - 'introspection'
